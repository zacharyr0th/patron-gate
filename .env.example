# ===========================================
# PatronGate Environment Variables
# Shelby (Storage) + Aptos (Blockchain)
# ===========================================
#
# Copy this file to .env and fill in your values:
# cp .env.example .env
#
# NEVER commit .env to git (it's in .gitignore)
# ===========================================

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Database (Neon Postgres)
# Get from: https://neon.tech
DATABASE_URL=postgresql://user:pass@host/database?sslmode=require

# ===========================================
# Aptos Configuration
# ===========================================

# Network (testnet or mainnet)
NEXT_PUBLIC_APTOS_NETWORK=testnet
NEXT_PUBLIC_APTOS_NODE_URL=https://fullnode.testnet.aptoslabs.com/v1

# Deployed Membership Contract Address
# Deploy the contract first, then set this
NEXT_PUBLIC_MEMBERSHIP_CONTRACT_ADDRESS=0x1

# Contract Deployment Keys (for deploying/upgrading contract)
APTOS_TESTNET_PRIVATE_KEY=your-private-key
APTOS_TESTNET_ADDRESS=your-address

# ===========================================
# Shelby Protocol (Decentralized Storage)
# ===========================================

# API Key (from https://geomi.dev)
SHELBY_API_KEY=your-shelby-api-key
SHELBY_NETWORK=SHELBYNET
NEXT_PUBLIC_SHELBY_NETWORK=SHELBYNET

# Payment recipient wallet (your Aptos address)
SHELBY_PAYMENT_RECIPIENT=your-aptos-address
NEXT_PUBLIC_PAYMENT_RECIPIENT=your-aptos-address

# Pricing (0.001 APT = 100000 octas per chunkset)
SHELBY_OCTAS_PER_CHUNKSET=100000

# Platform wallet for Shelby operations
MASTER_APTOS_PRIVATE_KEY=your-platform-private-key

# X402 Payment Processing (for Shelby sessions)
X402_FACILITATOR_PRIVATE_KEY=your-facilitator-private-key
X402_CONTRACT_ADDRESS=your-x402-contract-address
